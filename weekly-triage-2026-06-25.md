# Weekly Bug Triage — 2026-06-25

_Auto-generated by scheduled routine. Covers the 7-day window ending 2026-06-25._

---

## Git Activity (last 7 days)

**No commits in the past 7 days.** The most recent commit was `0b54845` on 2026-06-08 (17 days ago).

Last 5 commits for context:

| SHA | Date | Summary |
|-----|------|---------|
| `0b54845` | 2026-06-08 | Fix 4 bugs: backdrop close, ladder crash, wins blank title (#54) |
| `7b27281` | 2026-06-08 | Expand Work nav section by default so Givelink OS is always visible (#53) |
| `70d4241` | 2026-06-08 | Fix modal-open guard: prevent PTR firing behind dialogs (#52) |
| `eeb9b8f` | 2026-06-08 | Fix three bugs in Givelink dashboard (#51) |
| `67de902` | 2026-05-29 | Optional offline-first Supabase cloud sync + auto-snapshots (#50) |

---

## Code Quality Scan (full codebase)

### TODOs / FIXMEs / HACKs
**None found** in `index.html` or `givelink.html`. The codebase is clean of inline annotation debt.

### Console Statements
Found in `index.html` — all are intentional `console.warn` calls in error handlers (not debug leftovers):

| Line | Call | Context |
|------|------|---------|
| 2092 | `console.warn('theme media listener', e)` | Theme preference setup |
| 2107 | `console.warn('Corrupt localStorage...')` | State load fallback |
| 2953 | `console.warn('_wizSave error', e)` | Weekly review draft save |
| 3026 | `console.warn('fab action', e)` | FAB action wrapper |
| 9288 | `console.warn('Notification failed:', e)` | Browser notification |
| 9332 | `console.warn('ntfy connection failed:', e)` | ntfy.sh integration |
| 9433 | `console.warn('Notes synthesis parse failed:', e)` | AI response parsing |
| 9661 | `console.warn('Morning briefing cache error:', e)` | Briefing cache |

None of these are `console.log` debug statements — all appropriate. No `console.log` found in either file.

### Empty Catch Blocks
Found **8 empty catches** in `index.html` — these silently swallow errors:

| Line | Surrounding context |
|------|---------------------|
| 2433 | `localStorage` nav-collapse state persistence |
| 2501 | Weekly review draft banner render |
| 2877 | Weekly review wizard re-hydration |
| 3230 | Haptic feedback wrapper |
| 8624 | `refresh()` call inside event handler |
| 8657 | Nav state restoration |
| 10054 | `awardXP(15, 'eod')` — EOD ritual |
| 1083 | (givelink.html) Task operation guard |

**Recommendation:** Replace with `console.warn('[taskos]', e)` at minimum. See P2-5 in IMPROVEMENT_PLAN.md.

---

## Security Scan — Most Recent Commit (`0b54845`)

**Files changed:** `index.html` only (10 lines changed, 5 insertions / 5 deletions — all bug fixes).

### Secrets / Credentials
- No new secrets introduced.
- Existing Supabase anon key and URL remain in `index.html` (pre-existing, tracked in P3-3 of improvement plan).
- API key still acquired via `window.prompt()` and stored in `localStorage` (pre-existing, tracked in P1-3).

### XSS Risks
- **`toast()` in givelink.html uses `innerHTML`** (line 452 — pre-existing, tracked in P0-4).
- The four bug fixes in this commit (backdrop close, escape key, ladder crash, wins title) contain no new `innerHTML` assignments.
- No `eval()`, `Function()`, `document.write()`, or `setTimeout(string)` patterns found in changed lines.

### Injection Risks
- No new SQL or shell interpolation.
- Anthropic API calls continue to use `JSON.stringify` for request bodies — no interpolation risk.

**Verdict: Commit is safe. No new security issues introduced.**

---

## Outstanding Issues from Previous Triages

_No prior triage files found (this is the first run)._

---

## Action Items

Priority bugs requiring immediate fix (see `IMPROVEMENT_PLAN.md` for details):

1. **P0-1** — `callClaudeGL` no `res.ok` check → AI features fail silently on API errors
2. **P0-2** — Standup date off-by-one → standup generator shows tasks from 2 days ago
3. **P0-3** — Dynamic modals never get click-outside-to-close behavior
4. **P0-4** — `toast()` uses `innerHTML` → XSS vector

_Next triage scheduled: 2026-07-02_
